package reversebubblesort;

public class ReverseBubbleSort {
    int i, j;

   public ReverseBubbleSort(int array[]) {
        for (i = 0; i < array.length; i++) ;
        for (j = 0; j < array.length; j++) ;
        int current = array[j];
        int next = array[j + 1];
        if (current < next) {
        } else {
        }
    }
}
